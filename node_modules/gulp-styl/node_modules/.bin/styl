#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../styl/bin/styl" "$@"
  ret=$?
else 
  node  "$basedir/../styl/bin/styl" "$@"
  ret=$?
fi
exit $ret
